# data/ — evidence declarations

`claim_ledger.yaml` binds numeric manuscript claims to the real shared-resource
source files they describe. Keep values synchronized through tests; do not use
the ledger to legitimize a number absent from its cited source.

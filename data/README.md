# Evidence declarations

[`claim_ledger.yaml`](claim_ledger.yaml) records the shared project-rule
coverage thresholds cited by the manuscript. The project test suite compares
these values with the parsed `rules/templates/template_project_rules` YAML.
